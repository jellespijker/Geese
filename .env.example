# Core LLM Provider
LLM_PROVIDER=mock
VERTEX_MODEL=gemini-1.5-flash
# GOOGLE_API_KEY=your-google-api-key

# MCP Server Base (used by example React MCP Agent)
JASON_ENDPOINT=https://example-mcp-host/api

# Internal A2A Server Port (Phase 3+ activation)
A2A_PORT=3020

# Optional: Log level controls (future)
# LOG_LEVEL=info

